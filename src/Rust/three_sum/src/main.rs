fn main() {
    println!("Hello, world!");
}

pub fn three_sum(nums: Vec<i32>) -> Vec<Vec<i32>> {
    // fixed pointer
    let mut i = nums[0];

    for k in nums.iter().enumerate() {
        for j in nums.iter().enumerate() {}
    }
}
